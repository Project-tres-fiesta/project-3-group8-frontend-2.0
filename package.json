{
  "name": "eventlink-heroku-root",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "cd StickerSmash && npm install && npx expo export --platform web",
    "start": "node StickerSmash/server.js"
  }
}